name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: echo:"Welcome to first practice of github actions"
      - name: Print Goodbye
        run: echo "Done"
    
